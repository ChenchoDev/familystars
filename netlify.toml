[build]
  base = "apps/frontend"
  command = "npm install && npm run build"
  publish = "dist"

[dev]
  command = "cd apps/frontend && npm run dev"
  port = 3000

[context.production]
  environment = { VITE_API_URL = "https://familystars-api.onrender.com" }
